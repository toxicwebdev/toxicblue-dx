name: build-toxicblue-dx
on:
  schedule:
    - cron: "30 06 * * 1" # Создается в 6:30 UTC по Понедельникам

  push:
    branches-ignore: # Не перестраивать для dependabot
      - 'dependabot/**'
    paths-ignore: # Не перестраивать, если изменилась только документация
    - "**.md"

  workflow_dispatch: # возможность запускать сборки вручную

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

permissions: {}

jobs:
  bluebuild:
    name: Build toxicblue-dx
    runs-on: ubuntu-latest
    permissions:
      contents: read # требуется для настройки прав доступа к репозиторию
      packages: write # требуется для настройки прав доступа к репозиторию
      id-token: write # требуется для настройки прав доступа к репозиторию
    strategy:
      fail-fast: false # запретить GH отменять все сборки матрицы, если одна не удалась
      matrix:
        recipe:
          - recipe-hyprland-dx.yml
          - recipe-niri-dx.yml
          - recipe-river-dx.yml
          - recipe-sway-dx.yml
    steps:
      - name: Optimize build times
        shell: bash
        run: |
          echo 'set man-db/auto-update false' | sudo debconf-communicate
          sudo dpkg-reconfigure man-db
          sudo rm -f /var/lib/man-db/auto-update
          sudo sed -i 's/^update_initramfs=.*/update_initramfs=no/' /etc/initramfs-tools/update-initramfs.conf

      - name: Checkout repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Gather image data from recipe
        run: |
          echo "IMAGE_NAME=$(grep '^name:' ./recipes/${MATRIX_RECIPE} | sed 's/^name: //')" >> $GITHUB_ENV
          echo "IMAGE_MAJOR_VERSION=$(grep '^image-version:' ./recipes/${MATRIX_RECIPE} | sed 's/^image-version: //')" >> $GITHUB_ENV
          BASE_IMAGE=$(grep '^base-image:' ./recipes/${MATRIX_RECIPE} | sed 's/^base-image: //')
          echo "BASE_IMAGE_NAME=$(echo $BASE_IMAGE | sed 's/.*\/.*\///')" >> $GITHUB_ENV

        env:
          MATRIX_RECIPE: ${{ matrix.recipe }}

      - name: Verify base image
        uses: EyeCantCU/cosign-action/verify@58722a084c82190b57863002d494c91eabbe9e79 # v0.3.0
        with:
          containers: ${{ env.BASE_IMAGE_NAME }}:${{ env.IMAGE_MAJOR_VERSION }}
          registry: 'ghcr.io/toxicwebdev'
          pubkey: 'https://github.com/toxicwebdev/toxicblue/raw/main/cosign.pub'

      - name: Build toxicblue-dx
        uses: blue-build/github-action@3cd0c2835e691b87a7eb19261a6aee77a56169e2 # v1.11.0
        with:
          cli_version: v0.9.28 # если нужно использовать эту конкретную версию инструмента CLI
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
          build_chunked_oci: true
          max_layers: 128
          maximize_build_space: true # включено по умолчанию, отключите, если ваше изображение маленькое и вы хотите получить более быструю сборку
          squash: true
          skip_checkout: true
          use_cache: false
          retry_push_count: 3
          verify_install: true